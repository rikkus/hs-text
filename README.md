HS.Text collects text manipulation utilities.

FormatExtensions.Format ( "{greeting}, {planet}!", "greeting:Hello", "planet:world" );

FormatExtensions.MagicFormat ( "{Greeting}, {Planet}!", new { Greeting = "Hello", Planet = "world" } );
